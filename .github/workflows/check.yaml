name: check
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
      - run: nohup Xvfb &
          echo "DISPLAY=:0" >> $GITHUB_ENV
      - run: make check
